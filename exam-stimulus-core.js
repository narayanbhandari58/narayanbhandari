/* Central exam stimulus renderer v1.2.
   Runs synchronously with questionCard.innerHTML so DI/paragraph content is
   present before the browser paints. Uses the exact active paper saved by
   exam-resume.js; it never searches another exam or unrelated question.
*/
(function(){
  if(window.__NB_STIMULUS_CORE_V1)return;
  window.__NB_STIMULUS_CORE_V1=true;
  const KEY='nb_loksewa_exact_resume_v11';
  const OLD=['nb_loksewa_exact_resume_v10','nb_loksewa_exact_resume_v9','nb_loksewa_exact_resume_v8','nb_loksewa_exact_resume_v7','nb_loksewa_exact_resume_v6'];
  const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
  const readState=()=>{try{for(const k of [KEY,...OLD]){const x=JSON.parse(localStorage.getItem(k)||'null');if(x?.started&&Array.isArray(x.paper)&&x.paper.length)return x}}catch(e){}return null};
  const card=()=>document.getElementById('questionCard');
  const qIndex=el=>{const h=el?.querySelector('h2');const m=h?.textContent?.match(/^\s*(\d+)\s*[.)]/);return m?Number(m[1])-1:null};
  const pict=q=>{const t=String([q?.type,q?.format,q?.topic,q?.subject,q?.q].filter(Boolean).join(' ')).toLowerCase();return /pictorial|non-verbal|figure-count|triangle-count|आकृति|रेखा क्रम|घुमाइ|भुजा क्रम|भर्ने क्रम|दर्पण प्रतिबिम्ब|प्रतिबिम्ब/.test(t)};
  const di=q=>{if(!q||pict(q))return false;const t=String([q.type,q.category,q.topic,q.subject,q.unit].filter(Boolean).join(' ')).toLowerCase();return /data.?interpretation|डेटा व्याख्या/.test(t)||['pie-chart','bar-chart','line-graph'].includes(String(q.type||'').toLowerCase())};
  const stimulusKey=q=>{if(!q)return'';if(q.groupId)return'group:'+String(q.groupId);if(q.passage)return'passage:'+String(q.passage);if(q.data||q.figure)return'data:'+String(q.data||q.figure);return''};
  function sourceFor(q,st){
    if(!q)return null;
    if(q.groupId&&Array.isArray(st?.paper)){
      const same=st.paper.find(x=>String(x?.groupId||'')===String(q.groupId)&&!!(x?.passage||x?.data||x?.figure));
      if(same)return same;
    }
    return q;
  }
  function parse(q){const raw=String(q?.data||q?.figure||'').trim();if(!raw)return null;const type=String(q.type||'').toLowerCase();const parts=raw.split(/\s*;\s*/).map(x=>x.trim()).filter(Boolean);let title='',rows=[];
    if(type==='pie-chart'){
      let body=raw;const c=raw.indexOf(':');if(c>0){title=raw.slice(0,c).trim();body=raw.slice(c+1)}
      const re=/([A-Za-z][A-Za-z0-9 +&/_-]*?)\s*(\d+(?:\.\d+)?)%/g;let m;while((m=re.exec(body)))rows.push([m[1].trim(),m[2]+'%']);
    }else if(type==='line-graph'){
      parts.forEach((p,i)=>{const c=p.indexOf(':');let label='',body=p;if(c>0){label=p.slice(0,c).trim();body=p.slice(c+1).trim();if(i===0)title=label}const vals=body.split(/\s*[,|]\s*/).filter(Boolean);rows.push([label||String(i+1),...vals])});
    }else{
      parts.forEach((p,i)=>{const c=p.indexOf(':');let body=p,label='';if(c>0){if(i===0)title=p.slice(0,c).trim();body=p.slice(c+1).trim()}const m=body.match(/^([A-Za-z][A-Za-z0-9_-]*)\s+(.+)$/);if(m)rows.push([m[1],...m[2].split(/\s*[,|]\s*/).filter(Boolean)]);else rows.push(body.split(/\s*[,|]\s*/).filter(Boolean));});
    }
    return {type,title,rows};
  }
  function box(body){return `<div class="exam-stimulus central-stimulus" data-central-stimulus="1">${body}</div>`}
  function pie(d){const colors=['#ef4444','#f59e0b','#10b981','#3b82f6','#8b5cf6','#ec4899','#14b8a6','#f97316'];let a=0,total=d.rows.reduce((s,r)=>s+(parseFloat(r[1])||0),0);if(!total)return'';const stops=d.rows.map((r,i)=>{const v=parseFloat(r[1])||0,deg=v/total*360,z=`${colors[i%colors.length]} ${a}deg ${a+deg}deg`;a+=deg;return z}).join(',');const legend=d.rows.map((r,i)=>`<div class="central-legend"><i style="background:${colors[i%colors.length]}"></i><span>${esc(r[0])}</span><b>${esc(r[1])}</b></div>`).join('');return box(`${d.title?`<div class="central-title">${esc(d.title)}</div>`:''}<div class="central-pie"><div class="central-pie-chart" style="background:conic-gradient(${stops})"></div><div>${legend}</div></div>`)}
  function bar(d){const rows=d.rows.filter(r=>r.length>=2);if(!rows.length)return'';const vals=rows.map(r=>r.slice(1).map(v=>parseFloat(String(v).replace(/,/g,''))||0));const series=Math.max(1,...vals.map(v=>v.length)),max=Math.max(1,...vals.flat()),w=720,h=330,left=58,right=18,top=28,bottom=62,plotH=h-top-bottom,groupW=(w-left-right)/rows.length,barW=Math.min(34,(groupW-18)/series),colors=['#ef4444','#3b82f6','#10b981','#f59e0b','#8b5cf6'];let svg=`<svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Bar Chart" style="width:100%;height:auto;display:block"><line x1="${left}" y1="${top}" x2="${left}" y2="${h-bottom}" stroke="#94a3b8"/><line x1="${left}" y1="${h-bottom}" x2="${w-right}" y2="${h-bottom}" stroke="#94a3b8"/>`;for(let g=0;g<=4;g++){const y=top+plotH-plotH*g/4;svg+=`<line x1="${left}" y1="${y}" x2="${w-right}" y2="${y}" stroke="#e2e8f0"/><text x="${left-8}" y="${y+4}" text-anchor="end" font-size="12" fill="#64748b">${Math.round(max*g/4)}</text>`}rows.forEach((r,i)=>{const start=left+i*groupW+(groupW-vals[i].length*barW)/2;vals[i].forEach((v,j)=>{const bh=plotH*v/max,x=start+j*barW,y=top+plotH-bh;svg+=`<rect x="${x+2}" y="${y}" width="${Math.max(8,barW-5)}" height="${bh}" rx="5" fill="${colors[j%colors.length]}"/><text x="${x+barW/2}" y="${Math.max(18,y-5)}" text-anchor="middle" font-size="11" font-weight="700" fill="#334155">${v}</text>`});svg+=`<text x="${left+i*groupW+groupW/2}" y="${h-34}" text-anchor="middle" font-size="13" font-weight="700" fill="#334155">${esc(r[0])}</text>`});svg+='</svg>';return box(`${d.title?`<div class="central-title">${esc(d.title)}</div>`:''}${svg}`)}
  function line(d){if(d.rows.length<2)return'';const labels=d.rows[0].slice(1),series=d.rows.slice(1).map(r=>({name:r[0],vals:r.slice(1).map(v=>parseFloat(String(v).replace(/,/g,''))||0)})).filter(s=>s.vals.length);if(!labels.length||!series.length)return'';const w=720,h=330,left=58,right=18,top=28,bottom=62,plotW=w-left-right,plotH=h-top-bottom,max=Math.max(1,...series.flatMap(s=>s.vals)),colors=['#ef4444','#3b82f6','#10b981','#f59e0b','#8b5cf6'];let svg=`<svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Line Graph" style="width:100%;height:auto;display:block"><line x1="${left}" y1="${top}" x2="${left}" y2="${h-bottom}" stroke="#94a3b8"/><line x1="${left}" y1="${h-bottom}" x2="${w-right}" y2="${h-bottom}" stroke="#94a3b8"/>`;for(let g=0;g<=4;g++){const y=top+plotH-plotH*g/4;svg+=`<line x1="${left}" y1="${y}" x2="${w-right}" y2="${y}" stroke="#e2e8f0"/><text x="${left-8}" y="${y+4}" text-anchor="end" font-size="12" fill="#64748b">${Math.round(max*g/4)}</text>`}labels.forEach((lab,i)=>{const x=left+(labels.length===1?plotW/2:i*plotW/(labels.length-1));svg+=`<text x="${x}" y="${h-34}" text-anchor="middle" font-size="12" font-weight="700" fill="#334155">${esc(lab)}</text>`});series.forEach((s,si)=>{const pts=s.vals.map((v,i)=>{const x=left+(labels.length===1?plotW/2:i*plotW/(labels.length-1)),y=top+plotH-plotH*v/max;return[x,y,v]});svg+=`<polyline fill="none" stroke="${colors[si%colors.length]}" stroke-width="5" stroke-linecap="round" stroke-linejoin="round" points="${pts.map(p=>p[0]+','+p[1]).join(' ')}"/>`;pts.forEach(p=>svg+=`<circle cx="${p[0]}" cy="${p[1]}" r="6" fill="#fff" stroke="${colors[si%colors.length]}" stroke-width="4"/><text x="${p[0]}" y="${p[1]-11}" text-anchor="middle" font-size="11" font-weight="700" fill="#334155">${p[2]}</text>`)});svg+='</svg>';return box(`${d.title?`<div class="central-title">${esc(d.title)}</div>`:''}${svg}`)}
  function table(d){const rows=d.rows.filter(r=>r.length);if(!rows.length)return'';const n=Math.max(...rows.map(r=>r.length));const heads=Array.from({length:n},(_,i)=>i?'मान '+i:'विवरण');return box(`${d.title?`<div class="central-title">${esc(d.title)}</div>`:''}<div class="central-scroll"><table class="central-table"><thead><tr>${heads.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${heads.map((_,i)=>`<td>${esc(r[i]??'')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`)}
  function render(q,st){const d=parse(q);if(!d)return'';if(d.type==='pie-chart')return pie(d);if(d.type==='bar-chart')return bar(d);if(d.type==='line-graph')return line(d);return table(d)}
  function apply(el){if(!el||el.id!=='questionCard')return;const old=el.querySelectorAll('.data-stimulus:not([data-central-stimulus]),.live-di-stimulus');old.forEach(x=>x.remove());const st=readState(),i=qIndex(el),rawQ=st?.paper?.[i],q=sourceFor(rawQ,st);el.querySelectorAll('[data-central-stimulus]').forEach(x=>x.remove());if(!rawQ||!q)return;const h=el.querySelector('h2');if(!h)return;if(q.passage){h.insertAdjacentHTML('beforebegin',box(`<div class="central-title">अनुच्छेद</div><div class="central-passage">${esc(q.passage)}</div>`))}else if(di(q)){const html=render(q,st);if(html)h.insertAdjacentHTML('beforebegin',html)}}
  const desc=Object.getOwnPropertyDescriptor(Element.prototype,'innerHTML');if(!desc?.set)return;Object.defineProperty(Element.prototype,'innerHTML',{configurable:desc.configurable,enumerable:desc.enumerable,get:desc.get,set:function(v){desc.set.call(this,v);if(this.id==='questionCard')apply(this)}});
  const style=document.createElement('style');style.textContent='.central-stimulus{margin:14px 0;padding:14px;border:1px solid #d7dee8;border-radius:14px;background:linear-gradient(145deg,#fff,#f7f9fc);box-shadow:0 6px 18px rgba(15,23,42,.06)}.central-title{font-weight:850;color:#8f0e04;margin-bottom:12px}.central-passage{line-height:1.75}.central-scroll{overflow-x:auto;-webkit-overflow-scrolling:touch}.central-table{width:100%;border-collapse:separate;border-spacing:0;border:1px solid #d7dee8;border-radius:10px;overflow:hidden}.central-table th{padding:10px;background:#8f0e04;color:#fff;white-space:nowrap}.central-table td{padding:9px;border-bottom:1px solid #edf0f4;text-align:center}.central-table td:first-child{text-align:left;font-weight:650}.central-pie{display:grid;grid-template-columns:minmax(170px,260px) 1fr;gap:22px;align-items:center;max-width:720px;margin:auto}.central-pie-chart{width:min(260px,70vw);aspect-ratio:1;border-radius:50%;box-shadow:inset 0 0 0 16px #fff,0 8px 22px rgba(0,0,0,.12);margin:auto}.central-legend{display:flex;align-items:center;gap:8px;margin:7px 0}.central-legend i{width:13px;height:13px;border-radius:4px;display:block;flex:0 0 13px}.central-legend b{margin-left:auto}@media(max-width:600px){.central-pie{grid-template-columns:1fr;gap:14px}.central-pie-chart{width:min(230px,70vw)}}';document.head.appendChild(style);
  window.NB_applyExamStimulus=()=>apply(card());
})();