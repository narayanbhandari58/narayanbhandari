/*
  Baseline browser security headers.
  CSP is intentionally omitted for now because the existing site uses inline
  scripts/styles and third-party resources; adding an incomplete CSP could
  break production functionality.
*/

/*
  All static pages
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  X-Frame-Options: SAMEORIGIN
*/

/* Netlify _headers requires uncommented header blocks; keep this file minimal. */

/*
/* /
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  X-Frame-Options: SAMEORIGIN
*/
*/
